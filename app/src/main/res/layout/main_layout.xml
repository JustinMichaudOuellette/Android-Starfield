<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <android.opengl.GLSurfaceView
        android:id="@+id/gl_surface_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!--
        Debug-only FPS overlay. It stays GONE here and is revealed by
        MainActivity only when BuildConfig.DEBUG is true. It is neither
        clickable nor focusable so touches fall through to the surface view
        underneath, which drives speed and star density.
    -->
    <TextView
        android:id="@+id/fps_counter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_margin="16dp"
        android:clickable="false"
        android:focusable="false"
        android:fontFamily="monospace"
        android:shadowColor="#FF000000"
        android:shadowDx="1"
        android:shadowDy="1"
        android:shadowRadius="3"
        android:textColor="#FFFFFFFF"
        android:textSize="14sp"
        android:visibility="gone" />

</FrameLayout>
